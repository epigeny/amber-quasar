export function updateLoading (state, loading) {
  state.loading = loading
}

export function updateRealm (state, realm) {
  state.realm = realm
}

export function updateCredentials (state, credentials) {
  state.credentials = credentials
}

export function updateProfile (state, profile) {
  state.profile = profile
}
