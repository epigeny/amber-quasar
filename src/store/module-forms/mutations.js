export function updateLoading (state, loading) {
  state.loading = loading
}

export function updateFormGroups (state, formGroups) {
  state.formGroups = formGroups
}

export function updateFormValue (state, formValue) {
  state.formValue = formValue
}
